import type { NextRequest } from "next/server";import { z } from "zod";import { setPlatformUserStatus,type PlatformUserId } from "@tilbecore/platform";import { formObject,mutationFailure,mutationSuccess } from "../../../../../src/http";import { assertTrustedMutationRequest,platformRepository,requirePlatformActor } from "../../../../../src/platform-server";
export async function POST(request:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;try{await assertTrustedMutationRequest();const actor=await requirePlatformActor("platform.user.manage");const {status}=z.object({status:z.enum(["active","suspended"])}).parse(formObject(await request.formData()));await setPlatformUserStatus(platformRepository(),actor,id as PlatformUserId,status,new Date().toISOString());return mutationSuccess(request,"/users")}catch(error){return mutationFailure(request,error,"/users")}}
