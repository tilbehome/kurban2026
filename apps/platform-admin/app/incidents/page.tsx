import{AdminShell,JsonPanel,PageHead}from"../../src/components";import{pageActor}from"../../src/page-auth";import{platformRepository}from"../../src/platform-server";export const dynamic="force-dynamic";export default async function IncidentsPage(){await pageActor("platform.incident.read");const rows=await platformRepository().listIncidents();return <AdminShell><PageHead title="Platform Olay Merkezi" description="Olay oluşturma, durum geçişi, çözüm ve değiştirilemez zaman çizelgesi."/><section className="card"><h2>Yeni olay</h2><form className="form-grid" action="/api/incidents" method="post"><label>Önem<select name="severity"><option>info</option><option>warning</option><option>critical</option></select></label><label>Başlık<input name="title" required/></label><label>Mesaj<textarea name="message" required minLength={8}/></label><label>Etkilenen tenant ID’leri<input name="affectedTenantIds"/></label><button className="button">Olayı aç</button></form></section><section className="stack">{rows.length?rows.map((row,index)=><article className="card" key={String(row.id??index)}><JsonPanel value={row}/>{!["resolved","cancelled"].includes(String(row.status))?<form className="form-grid" action={`/api/incidents/${String(row.id)}/transition`} method="post"><input type="hidden" name="fromStatus" value={String(row.status)}/><label>Yeni durum<select name="toStatus"><option value="investigating">investigating</option><option value="resolved">resolved</option><option value="cancelled">cancelled</option></select></label><label>Güncelleme<textarea name="message" required/></label><button className="button">Zaman çizelgesine işle</button></form>:null}</article>):<div className="card">Açık veya tarihsel platform olayı yok.</div>}</section></AdminShell>}
