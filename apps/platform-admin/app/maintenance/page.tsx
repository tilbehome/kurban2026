import{AdminShell,JsonPanel,PageHead}from"../../src/components";import{pageActor}from"../../src/page-auth";import{platformRepository}from"../../src/platform-server";export const dynamic="force-dynamic";export default async function MaintenancePage(){await pageActor("platform.maintenance.read");const repo=platformRepository();const[windows,stops]=await Promise.all([repo.listMaintenanceWindows(),repo.listEmergencyStops()]);return <AdminShell><PageHead title="Bakım ve Acil Durdurma" description="Aktif politika tenant runtime tarafından fail-closed uygulanır; kritik yazılar sessizce devam etmez."/><div className="grid"><section className="card"><h2>Planlı bakım</h2><form className="form-grid" action="/api/maintenance" method="post"><label>Başlık<input name="title" required/></label><label>Kullanıcı mesajı<textarea name="message" required minLength={8}/></label><label>Tenant ID’leri<input name="affectedTenantIds"/></label><label>Başlangıç<input type="datetime-local" name="plannedStartAt" required/></label><label>Bitiş<input type="datetime-local" name="plannedEndAt" required/></label><label>Mod<select name="mode"><option value="read_only">Yalnız okuma</option><option value="full_stop">Tam durdurma</option></select></label><button className="button">Bakımı planla</button></form></section><section className="card"><h2>Acil durdurma</h2><form className="form-grid" action="/api/emergency-stops" method="post"><label>Organization ID<input name="organizationId" required/></label><label>Tenant ID<input name="tenantInstanceId"/></label><label>Modül ID<input name="moduleId"/></label><label>Durum<select name="status"><option value="active">active</option><option value="inactive">inactive</option></select></label><label>Mod<select name="mode"><option value="full_stop">Tam durdurma</option><option value="read_only">Yalnız okuma</option><option value="module_stop">Modül durdurma</option></select></label><label>Engelli scope’lar<input name="blockedScopes"/></label><label>Gerekçe<textarea name="reason" required minLength={8}/></label><button className="button danger">Politikayı kaydet</button></form></section></div><section className="stack">{windows.map((row,index)=><article className="card" key={String(row.id??index)}><JsonPanel value={row}/>{row.status==="planned"||row.status==="active"?<form action={`/api/maintenance/${String(row.id)}/transition`} method="post"><input type="hidden" name="fromStatus" value={String(row.status)}/><select name="toStatus">{row.status==="planned"?<><option value="active">Başlat</option><option value="cancelled">İptal</option></>:<option value="completed">Bitir</option>}</select><button className="button">Durumu uygula</button></form>:null}</article>)}{!windows.length?<div className="card">Planlı bakım kaydı yok.</div>:null}<h2>Acil durdurma kayıtları</h2>{stops.length?<JsonPanel value={stops}/>:<div className="card">Acil durdurma politikası yok.</div>}</section></AdminShell>}
