receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  memory_limiter:
    check_interval: 1s
    limit_mib: 384
  filter/drop_sensitive_attributes:
    error_mode: ignore
    traces:
      span:
        - 'attributes["http.request.header.authorization"] != nil'
        - 'attributes["db.connection_string"] != nil'
  attributes/redact:
    actions:
      - key: enduser.id
        action: delete
      - key: user.email
        action: delete
      - key: user.phone
        action: delete
      - key: db.connection_string
        action: delete
      - key: http.request.header.cookie
        action: delete
      - key: http.request.header.authorization
        action: delete
  batch: {}

exporters:
  prometheus:
    endpoint: 0.0.0.0:9464
  debug:
    verbosity: basic

service:
  telemetry:
    logs:
      level: warn
  pipelines:
    traces:
      receivers: [otlp]
      processors: [memory_limiter, filter/drop_sensitive_attributes, attributes/redact, batch]
      exporters: [debug]
    metrics:
      receivers: [otlp]
      processors: [memory_limiter, attributes/redact, batch]
      exporters: [prometheus]
